# Required - Meta Developer App Credentials
FACEBOOK_APP_ID=your_app_id
FACEBOOK_APP_SECRET=your_app_secret

# Server Configuration
TRANSPORT_TYPE=http          # "http" for MCP clients or "stdio" for Claude Desktop
PORT=3000                    # OAuth server port
HOST=localhost               # Server host
BASE_URL=http://localhost:3000   # Public URL for OAuth callbacks

# Optional
META_USER_AGENT=MetaGraphMCPServer/1.0.0
JWT_SECRET=                  # Auto-generated if not set

# Debug
DEBUG=false